source "https://rubygems.org"

# iOS release tooling (TestFlight lane — see specs/ios-testflight-cicd.md).
# Pinned exactly for reproducible CI resolution. Needs Ruby >= 3.0 — the macOS
# system Ruby 2.6 is too old; use rbenv locally. CI pins Ruby 3.3.
gem "fastlane", "2.236.1"
