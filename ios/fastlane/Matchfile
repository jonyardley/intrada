git_url(ENV["MATCH_GIT_URL"] || "https://github.com/jonyardley/intrada-certificates")
storage_mode("git")

type("appstore")
app_identifier("com.intrada.app")

# Use App Store Connect API key for auth (no Apple ID needed)
# Keys are passed via environment variables in CI:
#   APP_STORE_CONNECT_API_KEY_KEY_ID
#   APP_STORE_CONNECT_API_KEY_ISSUER_ID
#   APP_STORE_CONNECT_API_KEY_KEY
